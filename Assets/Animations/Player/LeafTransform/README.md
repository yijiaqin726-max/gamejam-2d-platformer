# Player Leaf Transform

`Frames/` contains the ordered 20-frame sprite sequence for the player leaf transform animation.

Use the frames in numeric order from `player_leaf_transform_01.png` to `player_leaf_transform_20.png`.

`Reference/` keeps the original short reference video.
